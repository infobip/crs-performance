# OpenAI-compatible LiteLLM gateway used by the API evaluation notebooks.
# The notebooks append /v1 when it is not already present.
LITELLM_ENDPOINT=https://cody.ib-inet.com/

# Replace this placeholder with your LiteLLM API key in .env.
LITELLM_API_KEY=...
